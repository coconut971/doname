{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "doname": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--locked", "--project", "${PLUGIN_ROOT}", "--", "python", "-m", "doname.mcp_server"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
